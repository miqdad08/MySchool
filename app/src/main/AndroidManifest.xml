<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appidn">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GuruActivity"></activity>
        <activity android:name=".MusyrifActivity" />
        <activity android:name=".SiswaAsramaActivity" />
        <activity android:name=".SiswaActivity" />
        <activity android:name=".TentangActivity" />
        <activity android:name=".JadwalActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AsramaActivity" />
        <activity android:name=".KelasActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>